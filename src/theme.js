import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
      primary: {
        main: '#49d0be',
        light: '#ffffff',
      },
      secondary: {
        main: '#e91e63',
      },
    },
});

export default theme;
